[Unit]
Description=Ouroboros daily audit timer (01:30)
Requires=ouroboros-audit.service

[Timer]
OnCalendar=*-*-* 01:30:00
Unit=ouroboros-audit.service
Persistent=true
AccuracySec=1s

[Install]
WantedBy=timers.target
